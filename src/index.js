import React from 'react';
import ReactDOM from 'react-dom';
import './app.css'; // If you have an index.css file, otherwise ignore this line
import App from './app';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
